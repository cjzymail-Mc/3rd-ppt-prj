<!-- MODE: free_form -->
1、参考文本（参考语调和信息密度）
{style_anchor}

2、你的任务
请从{n}名测试者的实际反馈中，自由归纳这款篮球鞋的【{focus}】。
根据实际反馈内容自行决定分段维度，不要按固定性能类别（如包裹性、止滑性等）分类。
在每条结论后注明（X/{n}）表示几分之几的测试者有此体验。
每段结论中，请将最核心的1-2个关键性能词用【】括起来（仅括词本身，不含标点），例如：【止滑性】在室内场地表现稳定（2/{n}）。这些关键词后续会自动高亮显示。
{user_instruction}
注意：
- 你只能分析已有数据，不能推测或编造
- 直接给出结论，不要展示分析过程
- 参考文本仅作语调参考，不必复制其分类结构
- 总字数控制在{target_chars}字左右
- 不超过{max_lines}行
- 每个分类不超过3行
- 关键词要求：开头第一句必须同时包含'样本'和'反馈'（如"本次{n}名样本反馈显示"），正文中自然融入'建议'（如"建议关注..."）

3、{n}名测试者原始反馈
{respondent_block}

直接输出结论，不需要任何前言。

<!-- MODE: categorized -->
1、参考文本（参考语调和信息密度）
{style_anchor}

2、你的任务
下面是{n}名测试者对这款篮球鞋的原始试穿反馈，请帮我按分类汇总其中的【{content_source}】。
在每条结论后注明（X/{n}）表示几分之几的测试者有此反馈。
{user_instruction}
注意：
- 你只能分析已有数据，不能推测或编造
- 直接给出结论，不要展示分析过程
- 严格按照参考文本的格式、语调、陈述方式
- 总字数控制在{target_chars}字左右
- 不超过{max_lines}行
- 每个分类不超过3行
- 关键词要求：开头第一句必须同时包含'样本'和'反馈'（如"本次{n}名样本反馈显示"），正文中自然融入'建议'（如"建议关注..."）

3、{n}名测试者原始反馈
{respondent_block}

直接输出结论，不需要任何前言。
