请从最近的任务中总结经验教训，特别是处理过程中遇到的错误、试错步骤和最终成功的窍门/捷径。然后，将这些内容更新到CLAUDE.md文件的“2. 已知错误与教训”部分（如果合适，追加新小节）。确保更新是简洁的、结构化的（如用bullet points），并在“4. 变更记录”中添加一条记录（格式：YYYY-MM-DD：新增/更新了XXX问题的工作around）。

具体步骤：
- 先读取CLAUDE.md的当前内容。
- 分析最近任务：例如，如果涉及Excel中文问题，记录如“处理含中文的Excel文件时，用pandas读写需指定encoding='utf-8'；避免直接open()读大文件，用pd.read_excel()”。
- 只添加新/相关的经验，避免重复。
- 更新后，告诉我修改了哪些部分。
- 严格用相对路径操作文件（参考CLAUDE.md的路径规则）。