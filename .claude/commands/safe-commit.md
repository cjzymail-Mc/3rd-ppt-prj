请严格按照以下步骤操作，不要push到远程：
- 先运行 git add . 来添加所有修改。
- 如果用户没有提供提交消息（$1 为空），先分析当前变更（用 git status 或 git diff --cached 总结修改的文件和内容），然后自动生成一个简洁的提交消息摘要（中文，基于变更，如 "将xx问题的解决方案更新至 CLAUDE.md"），并询问用户确认或修改（例如："建议消息：'xx问题修复更新'。确认吗？或提供你的消息。"）。
- 如果用户提供了 $1 或确认了建议，则运行 git commit -m "$1"（用确认的消息）。
- 完成后告诉我结果，但绝对不要执行 git push。